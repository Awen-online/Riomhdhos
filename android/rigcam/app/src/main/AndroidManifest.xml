<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- WAKE_LOCK + a high-perf WifiLock are not optional here: measured on the Pixel 6,
         WiFi packet loss went from ~0% to 37.8% the moment the screen locked. A camera
         that drops a third of its frames when you stop touching the phone is useless. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- ⚠️ FOREGROUND_SERVICE_CAMERA is mandatory from Android 14 for a service that holds
         the camera, and must match foregroundServiceType below. Missing it does not fail
         quietly - startForeground throws and the service dies immediately. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <!-- Optional at runtime: audio is off by default and a denial must never stop the
         camera. See CamService.updateForegroundType - the service claims CAMERA alone at
         startup and only adds MICROPHONE once the mic is genuinely running, because the
         no-type startForeground overload asserts every type declared below and would kill
         the service outright on a phone that never granted this. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-feature android:name="android.hardware.camera.any" android:required="true" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.RigCam">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="landscape"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".CamService"
            android:exported="false"
            android:foregroundServiceType="camera|microphone" />
    </application>
</manifest>
